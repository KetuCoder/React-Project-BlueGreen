apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo-blue
spec:
  replicas: 3
  selector:
    matchLabels:
      app: todo-blue
  template:
    metadata:
      labels:
        app: todo-blue
        version: blue
    spec:
      containers:
      - name: todo-blue
        image: cloudketan/todo:blue
        ports:
        - containerPort: 80
